APP_ENV=testing
APP_URL=http://localhost
DB_CONNECTION=sqlite
DB_DATABASE=:memory:
DB_URL=
CACHE_STORE=array
SESSION_DRIVER=array
QUEUE_CONNECTION=sync
# tests/bootstrap.php selects a process-specific, absent APP_CONFIG_CACHE.
# MySQL requires the disposable harness; never point these at a persistent DB.
